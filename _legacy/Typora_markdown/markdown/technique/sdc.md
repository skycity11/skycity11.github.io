## set multicycle path

```verilog
 set_multicycle_path 2 -setup -from [get_pins <begin point>] -to [get_pins <end_point>]
 set_multicycle_path 1 -hold  -from [get_pins <begin point>] -to [get_pins <end_point>]
```

